{
  "name": "exocore-2",
  "version": "1.0.0",
  "description": "Exocore-2 Jekyll site",
  "scripts": {
    "build": "rm -f Gemfile.lock && bundle install && bundle exec jekyll build"
  },
  "engines": {
    "node": "16.x",
    "npm": "8.x"
  }
} 
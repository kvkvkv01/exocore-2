{
  "name": "exocore-2",
  "version": "1.0.0",
  "description": "Exocore Jekyll site",
  "scripts": {
    "install": "apt-get update && apt-get install -y ruby-full build-essential",
    "build": "chmod +x netlify-build.sh && ./netlify-build.sh"
  },
  "engines": {
    "node": "16.x"
  }
} 